# Program to add two numbers

def add_numbers(a,b):
    z=a+b
    return z
add_numbers(5, 5)

add_numbers(15, 5)

!git add add_numbers.py
!git commit -m "My commit 1"
!git remote add origin https://github.com/ussozi/GIT_trial.git

!git pull origin master

!git push -u origin master

!git push origin master
